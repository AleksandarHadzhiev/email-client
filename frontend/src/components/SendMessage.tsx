//@ts-ignore
export default function SendMessage({ setSendMessageIsActivated }) {
    return (
        <section>
            <button onClick={() => { setSendMessageIsActivated(false) }}>Close</button>
            {/* Topbar */}
            {/* Body */}
            {/* Footer */}
            <p>Meow</p>
        </section>
    )
}